<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="description" content="">
  <meta name="author" content="">
  <title>test_app</title>

  <!-- Bootstrap core CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      /* background-color: #f5f5f5; */
    }
    .form-container {
      width: 100%;
      max-width: 400px;
      padding: 15px;
      margin: auto;
      margin-top: 50px;
      background-color: #f5f5f5;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }
    .form-control {
      position: relative;
      box-sizing: border-box;
      height: auto;
      padding: 10px;
      font-size: 16px;
      margin-bottom: 15px;
    }
    .form-control:focus {
      z-index: 2;
    }
    .small-button {
      width: 100px;  /* Set the width */
      height: 40px;  /* Set the height */
    }
    /* Custom CSS to reduce spacing between buttons */
    .button-container {
      display: flex;
      justify-content: space-between;
    }
  </style>
</head>
<body class="text-center">
  <div class="container">
    <div class="form-container">
      <%= form_with(url: login_path, local: true, class: 'form-signin') do |form| %>
        <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
        
        <%= form.label :username, 'Username', class: 'sr-only' %>
        <%= form.text_field :username, class: 'form-control', placeholder: 'Username', required: true, autofocus: true %>
        
        <%= form.label :password, 'Password', class: 'sr-only' %>
        <%= form.password_field :password, class: 'form-control', placeholder: 'Password', required: true %>
        
     <div class="button-container d-flex justify-content-center">
  <%= form.submit 'Sign in', class: 'btn btn-primary mr-2' %>
  <button class="btn btn-secondary" type="button" onclick="window.location.href='/users/new'">Sign up</button>
</div>






      <% end %>
      <p class="mt-5 mb-3 text-muted text-small">&copy; Jadan Morrow</p>
    </div>
  </div>
</body>
</html>
